apiVersion: v1
kind: Service
metadata:
  name: multi-db-backend
  namespace: multi-db-app
spec:
  selector:
    app: multi-db-backend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
